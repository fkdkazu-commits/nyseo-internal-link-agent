# NYSEO内部リンクAIエージェント 要件定義書

> 作成日: 2026-05-16

---

## 1. プロジェクト概要

### 目的
NYSEO出力データをもとに、被リンク不足記事（被リンク数1件以下）に対して、関連性の高い他記事から内部リンクを設置する候補を自動提案し、SEO内部リンク構築業務を半自動化する。

### 主要機能
- 被リンク不足記事の自動抽出（E列 ≤ 1）
- メインKW（C列）を起点とした関連記事探索（KW一致・部分一致・関連語・共起語・類義語）
- 候補記事のHTML解析（title / h1 / h2 / h3 / 本文）
- Cowork内蔵AIによる関連性スコアリング（100点満点・5観点）
- 内部リンク設置候補のCSV出力（H列以降）

> **本システムは「提案型」MVPです。最終的な内部リンク設置はクライアント担当者様が確認・判断することを想定しています。WordPressへの自動反映は本フェーズでは対象外となります。**

---

## 2. ユーザー・権限定義

| ロール | 説明 | できること | できないこと |
|---|---|---|---|
| システム運用者（エージェント実行者） | Cowork上でエージェントを起動する | エージェント実行・再実行・ログ確認 | WordPressへの直接投稿（本フェーズ対象外） |
| クライアント担当者（レビュアー） | 提案結果を確認・レビューする | 出力CSV確認・内部リンク実施の最終判断 | エージェントの直接操作 |

---

## 3. 機能要件

### 3.1 STEP1 — 対象記事抽出
- 入力CSVのE列（被リンク数）を全行読み取る
- E列 ≤ 1 かつ H列が空白の行を対象に選定する
- 対象行のB列（記事URL）・C列（メインKW）を取得する
- C列が空白の場合はB列のURLから記事を取得し、`<title>` / `<h1>` / `<h2>` / `<h3>`（本文見出し全体）を解析してメインKWを自動検出する（全記事を処理対象にするため）
- B列が空の場合のみその行をスキップしてログに記録する

### 3.2 STEP2 — 関連記事候補探索
- メインKW（C列またはtitle/h1/h2/h3から自動検出）とCSV内の全記事KWをKW完全一致・部分一致で比較する
- Cowork内蔵AIを用いてKWの関連語・共起語・類義語を生成し探索範囲を拡張する
- 対象記事自身のURL・すでに採用済みのURLは除外する
- スコア順に最大20件の候補リストを生成してSTEP3へ渡す

### 3.3 STEP3 — 候補記事取得・解析
- 各候補記事URLへHTTPSリクエストを送信する（タイムアウト10秒）
- BeautifulSoupで以下を抽出する
  - `<title>` タグ
  - `<h1>` タグ
  - `<h2>` / `<h3>` タグ一覧
  - 本文テキスト（先頭800文字程度）
- HTTP失敗（404・タイムアウト等）はスキップ・警告ログ出力・処理継続する
- 最大2回リトライ後に失敗した場合は候補から除外する

### 3.4 STEP4 — AI関連性判定
- Cowork内蔵AIを使用し、5観点で関連性を100点満点でスコアリングする
- スコア ≥ 60点の記事を採用候補とする
- 候補が2件未満の場合は閾値を40点に下げて再判定する
- 採用候補から「貼る箇所の見出し」もAIが合わせて提案する

### 3.5 STEP5 — 内部リンク設置候補生成・出力
- 採用候補をスコア降順で並び替え、上位2〜3件を出力CSVに書き込む
- 出力列:
  - H列: 候補記事URL①
  - I列: リンク設置箇所見出し①
  - J列: 候補記事URL②
  - K列: リンク設置箇所見出し②
  - L列: 候補記事URL③（任意）
  - M列: リンク設置箇所見出し③（任意）

---

## 4. ビジネスロジック・業務ルール

### 抽出ルール
- E列 ≤ 1（E列が空白の場合も対象に含める）
- 同一URLの重複処理は行わない

### 候補生成ルール
- 同一URLへの重複提案は除外する
- 対象記事自身のURLは除外する
- 提案は最低2件・最大3件（MVPフェーズ）

### 出力保護ルール
- H列にデータがある行はスキップする（再取り込み時も同様）

---

## 5. 非機能要件

| 項目 | 内容 |
|---|---|
| 対象デバイス | PC（Cowork実行環境） |
| デザイン優先度 | 低（出力はCSV） |
| 同時利用人数 | 1名（シングルエージェント実行） |
| 処理速度 | 1記事あたり最大30秒以内（AI・HTTP取得込み） |
| 認証方式 | 不要（Cowork内蔵AI使用のため） |
| 通信 | HTTPS（外部URL取得） |
| エラー耐性 | 個別記事のHTTP失敗は警告ログを出してスキップ。全体処理は継続する |
| デプロイ先 | Cowork（クラウド実行環境） |

---

## 6. 制約条件

| 項目 | 内容 |
|---|---|
| インフラ | AWSなどの大規模インフラは使用しない |
| 実行環境 | Cowork上で完結させる |
| 言語 | Python 3.10以上（BeautifulSoup / requests） |
| AI | Cowork内蔵AI（Claude搭載） |
| データ基盤 | CSV（NYSEO出力ファイル） |
| WordPress自動投稿 | 本フェーズ対象外 |
| アンカーテキスト自動生成 | 本フェーズ対象外 |
| Embedding活用 | 本フェーズ対象外 |

---

## 7. 未確定事項

| # | 項目 | 優先度 | 対応方針 |
|---|---|---|---|
| 1 | 対象記事がクロール可能かどうか | 高 | クライアントに確認が必要。非公開・Basic認証記事は取得不可 |
| 2 | 候補記事の最大探索件数 | 中 | MVP段階では全記事を対象（上限は後から設定可能） |
| 3 | エラーログの保存先 | 低 | Cowork実行ログに出力する形でよい |

---

## 8. クライアント側の準備事項

以下の準備が完了していないとシステムは動作しないため、事前にご確認ください。

| 項目 | 内容 | 確認状況 |
|---|---|---|
| Cowork環境 | Coworkアカウントの用意・エージェント実行権限の確認 | 要確認 |
| 入力CSV | NYSEOから出力したCSVファイルの用意 | 要確認 |
| 記事のアクセス可否 | B列の記事URLがHTTPSで公開済みであること（非公開・Basic認証不可） | 要確認 |

---

## 9. ヒアリングメモ（インプット情報）

```
【プロジェクト背景】
- クライアント依頼による新規開発案件
- NYSEO出力データを活用した内部リンク構築業務の効率化

【主要要件まとめ】
- 対象: E列 ≤ 1 の被リンク不足記事
- 処理: メインKWから関連記事を探索しAIでスコアリング
- 出力: 出力CSVのH列以降に候補記事URL・見出しを出力
- 運用: 提案型MVP、最終判断は人間

【技術スタック（確定）】
- Cowork（内蔵AI活用） / Python + BeautifulSoup + requests / CSV
- AWSなどの大規模インフラは不使用

【将来拡張（本フェーズ対象外）】
- WordPressへの自動反映
- アンカーテキスト自動生成
- Embeddingを用いた高度関連記事探索
- Search Console連携
```
