# NYSEO内部リンクAIエージェント 設計書 v2

> 作成日: 2026-05-17 / バージョン: v2（Google Sheets方式・複数ユーザー共有）
> v1との違い: データ基盤がGoogle Spreadsheet、article_cacheタブによるキャッシュ共有、AI判定を上位概念→下位詳細パターン特化に変更

---

## 1. 技術スタック

| カテゴリ | 技術 | バージョン | 選定理由 |
|---|---|---|---|
| エージェント実行環境 | Cowork | 最新 | クライアント要件・ノーインフラ運用 |
| AI / LLM | Cowork内蔵AI（Claude搭載） | — | 追加API費用なし・Cowork Proプランに含まれる |
| スクリプト言語 | Python | 3.10以上 | gspread・BeautifulSoup・requests全対応 |
| HTML解析 | BeautifulSoup4 | 4.12.x | 軽量・安定した記事解析・日本語対応 |
| HTTPクライアント | requests | 2.31.x | シンプルな外部URL取得・セッション管理 |
| データ基盤 | Google Spreadsheet | — | 複数ユーザー共有・キャッシュを永続化できる |
| Sheetsクライアント | gspread | 6.0.0以上 | Python製Google Sheets APIラッパー |
| Google認証 | google-auth | 2.0.0以上 | サービスアカウントJSONによる認証 |

---

## 2. システム構成

```
[クライアント]
    ↓ Coworkのチャット欄にSpreadsheet URLを伝える
[Cowork + Pythonスクリプト（py main.py <URL>）]
    ├─ SheetsClient: Spreadsheet接続・データ読み込み・キャッシュ参照
    ├─ STEP1: 対象記事抽出（E列≤1 かつH列空）
    ├─ STEP3: 全記事HTML取得（article_cacheタブ優先 → HTTP取得）
    ├─ STEP2: 関連記事候補探索 ──→ [Cowork内蔵AI]（上位概念KW生成）
    ├─ STEP4: 関連性判定 ──→ [Cowork内蔵AI]（上位→下位パターン特化スコアリング）
    └─ STEP5: Spreadsheetへ書き込み（H〜M列）
         ↓
[Google Spreadsheet（共有）]
    ├─ Sheet1: NYSEOデータ + H〜M列に提案結果
    └─ article_cache: URL・title・h1・h2_json・h3_json・body_text・fetched_at
         ↓
[クライアント担当者が全員でSpreadsheetを確認・最終判断]
[担当者がWordPressに手動で内部リンクを設置]
```

---

## 3. モジュール構成・ディレクトリ設計

```
nyseo-internal-link-agent/
├── main.py                    # エントリーポイント: py main.py <SpreadsheetURL>
├── config.py                  # 設定値（閾値・列番号定義）
├── requirements.txt           # gspread・google-auth・requests・beautifulsoup4
├── .gitignore
│
├── steps/
│   ├── __init__.py
│   ├── step1_extract.py       # STEP1: 対象記事抽出（E列≤1・H列空）
│   ├── step2_search.py        # STEP2: KW一致・上位概念展開での候補探索
│   ├── step3_fetch.py         # STEP3: HTTP取得・BeautifulSoup解析
│   ├── step4_judge.py         # STEP4: AI関連性判定（上位→下位特化）
│   └── step5_output.py        # STEP5: Sheets書き込み用データ整形
│
├── utils/
│   ├── __init__.py
│   ├── sheets_client.py       # Google Sheets読み書き・article_cacheタブ管理
│   ├── ai_client.py           # Cowork内蔵AI（claude CLI）呼び出しラッパー
│   └── logger.py              # ログ出力ユーティリティ
│
└── prompts/
    ├── kw_expansion.txt       # STEP2: 上位概念KW生成プロンプト
    └── relevance_judge.txt    # STEP4: 上位→下位パターン特化判定プロンプト
```

---

## 4. データフロー

### Google Spreadsheetのタブ構成

**Sheet1（データタブ・1枚目）**

| 列 | 内容 | 型 | 操作 |
|---|---|---|---|
| A列 | NO（連番） | integer | 読み取りのみ |
| B列 | 記事URL | string（URL） | 読み取りのみ |
| C列 | メインKW（GSCクリック最多） | string | 読み取りのみ（空白可） |
| D列 | 発リンク数 | integer | 読み取りのみ |
| E列 | 被リンク数 | integer | 読み取りのみ（≤1が処理対象） |
| F列 | クリック数 | integer | 読み取りのみ |
| G列 | 表示回数 | integer | 読み取りのみ |
| H列 | 候補記事URL① | string（URL） | 書き込み |
| I列 | リンク設置箇所見出し① | string | 書き込み |
| J列 | 候補記事URL② | string（URL） | 書き込み |
| K列 | リンク設置箇所見出し② | string | 書き込み |
| L列 | 候補記事URL③（任意） | string（URL） | 書き込み |
| M列 | リンク設置箇所見出し③（任意） | string | 書き込み |

**article_cacheタブ（2枚目・自動作成）**

| 列 | 内容 |
|---|---|
| url | 記事URL（主キー） |
| title | titleタグのテキスト |
| h1 | h1タグのテキスト |
| h2_json | h2見出し一覧（JSON配列文字列） |
| h3_json | h3見出し一覧（JSON配列文字列） |
| body_text | 本文テキスト（先頭1500文字） |
| fetched_at | 取得日時（YYYY-MM-DD HH:MM:SS） |

### 中間データ（メモリ内）

```python
# SheetsClientのキャッシュ（起動時にarticle_cacheタブから読み込む）
_cache: dict[str, dict] = {
    "https://example.com/article/": {
        "url": "https://example.com/article/",
        "title": "記事タイトル",
        "h1": "見出しH1",
        "h2_list": ["見出しA", "見出しB"],
        "h3_list": ["見出しD", "見出しE"],
        "body_text": "本文テキスト...",
        "kw": "C列のKW（処理時に付与）"
    }
}

# AI判定結果（STEP4出力）
judgment_result = {
    "url": "https://example.com/article/",
    "score": 85,
    "reason": "候補記事は風俗の種類全般を解説した上位概念記事であり、対象記事（デリヘル特化）への誘導が自然。",
    "recommended_heading": "デリヘルとは"
}
```

---

## 5. 外部連携

| 外部サービス | 用途 | 認証 | 注意事項 |
|---|---|---|---|
| Google Sheets API | データ読み込み・結果書き込み・キャッシュ保存 | サービスアカウントJSON（プロジェクト外保管） | 環境変数 `GOOGLE_SERVICE_ACCOUNT` でパス指定 |
| Cowork内蔵AI | STEP2 KW拡張・STEP4 関連性判定 | 不要（Cowork Proプランに含まれる） | `claude -p <prompt>` でCLI呼び出し・encoding="utf-8"必須 |
| 対象サイト（HTTPS） | STEP3 記事HTML取得 | なし（公開ページのみ） | タイムアウト10秒・リトライ2回・UA設定必須 |

---

## 6. SheetsClientクラス設計

```python
class SheetsClient:
    def __init__(self, spreadsheet_url: str):
        # サービスアカウントJSONで認証・Spreadsheetに接続
        # article_cacheタブを取得 or 自動作成
        # キャッシュをメモリに読み込む

    def load_data(self) -> tuple[list[str], list[list[str]]]:
        # Sheet1（1枚目）の全データを返す

    def get_cache(self, url: str) -> dict | None:
        # URLのキャッシュを返す。なければNone

    def save_cache(self, article: dict) -> None:
        # article_cacheタブに追記。既存URLはスキップ

    def write_result(self, row_idx: int, result_row: list[str]) -> None:
        # Sheet1のH〜M列（row_idx+2行目）に書き込む
```

---

## 7. AI呼び出し設計

### Cowork内蔵AI（claude CLI）

```python
result = subprocess.run(
    ["claude", "-p", prompt, "--output-format", "text"],
    capture_output=True,
    text=True,
    encoding="utf-8",   # Windows (cp932) 対策で必須
    timeout=CLI_TIMEOUT,
)
```

### STEP2: KW拡張プロンプト（上位概念特化）

```
あなたはSEOの内部リンク構造を最適化する専門家です。
以下のメインキーワードに対して、「このキーワードの記事が特集ページとして含まれるような
上位概念・まとめ記事」で使われそうなキーワードを5〜10個生成してください。

メインKW: {target_kw}

出力形式（JSON配列）:
["上位概念KW1", "上位概念KW2", ...]

注意:
- 同階層・同テーマの記事（例: 別の地域の同ジャンル記事）はNGです
- 対象記事の内容を「まとめる側」「紹介する側」になるKWを出力してください
- 日本語で出力すること
```

### STEP4: 関連性判定プロンプト（上位→下位パターン特化）

```
あなたはSEOの内部リンク構造を最適化する専門家です。

## 内部リンクの方向
候補記事（リンクを貼る側）→ 対象記事（リンク先・被リンクを増やしたい記事）

## 対象記事（被リンクを増やしたい記事）
- KW: {target_kw}
- URL: {target_url}

## 候補記事（リンクを設置する側）
- タイトル: {candidate_title}
- URL: {candidate_url}
- 主要見出し: {candidate_headings}
- 本文冒頭: {candidate_body}

## 採点基準（合計100点）
1. 候補記事が対象記事の「上位概念・まとめ記事」になっているか（40点）
   - 高得点: 候補記事が「種類まとめ」「ガイド」「特集」で、対象記事がその1要素
   - 低得点: 同じジャンルの別地域・別サービスの横並び記事（0〜10点）
2. 候補記事の読者が対象記事を読みたくなるか（30点）
   - 候補を読んで「もっと詳しく知りたい」と思ったとき対象記事が答えになるか
3. 候補記事の文脈に対象記事のトピックが自然に含まれるか（30点）
   - 候補記事内に対象記事のKWや内容が話題として登場するか

## 出力形式（JSONのみ・余分なテキスト不要）
{"score": 整数(0-100), "reason": "判定理由（1〜2文）", "recommended_heading": "候補記事内でリンクを設置するのに最適な見出し"}
```

---

## 8. エラー処理

| エラー種別 | 対応 | 処理継続 |
|---|---|---|
| Google Sheets接続失敗 | エラーログ出力・実行停止（環境変数・権限を確認） | ❌ 停止 |
| HTTP取得失敗（404・タイムアウト等） | 該当記事をスキップ・警告ログ出力 | ✅ 継続 |
| Cowork内蔵AI エラー | 最大2回リトライ後、スキップ・エラーログ | ✅ 継続 |
| JSON解析失敗（AI出力不正） | デフォルト値（スコア0）で処理継続 | ✅ 継続 |
| 候補記事が2件未満 | 閾値を60→40に下げて再判定。不足なら空欄出力 | ✅ 継続 |
| B列（記事URL）が空白 | 該当行をスキップ・警告ログ | ✅ 継続 |
| H列にデータあり（処理済み） | 処理済みとして完全スキップ | ✅ 継続 |

---

## 9. 処理フロー

### 9.1 main.py処理順序

```
1. SheetsClient初期化（Sheets接続・article_cacheタブ確認・キャッシュ読み込み）
2. Sheet1のデータ全件読み込み
3. STEP1: E列≤1 かつ H列空の対象行を抽出
4. 全記事のHTML取得ループ（キャッシュ優先）
   └─ キャッシュあり → そのまま使用
   └─ キャッシュなし → HTTP取得 → article_cacheタブに保存
5. 対象記事ごとの処理ループ
   ├─ C列空の場合: h2/h3見出しをKWとして使用
   ├─ STEP2: KW一致 + AI上位概念展開で候補探索
   ├─ STEP4: AI関連性スコアリング
   └─ STEP5: SpreadsheetのH〜M列に書き込み
```

### 9.2 クライアント運用フロー

| 手順 | 担当 | 操作内容 |
|---|---|---|
| ① | クライアント | NYSEOデータをSpreadsheetに貼り付け |
| ② | クライアント | サービスアカウントをSpreadsheetに共有（編集者） |
| ③ | 運用者 | Coworkのチャット欄にSpreadsheet URLを送信 |
| ④ | 運用者 | `py main.py <URL>` でエージェント起動 |
| ⑤ | システム（自動） | STEP1〜5を自動実行し、H〜M列に提案結果を書き込む |
| ⑥ | 全員 | SpreadsheetのH列以降を確認・レビュー |
| ⑦ | クライアント | レビューOKの候補記事（H列）を開き、I列見出し箇所に手動でリンクを設置 |
| ⑧ | 運用者 | 再実行の場合はH〜M列のデータを削除して再実行 |

---

## 10. 開発フェーズ

### Phase 1: 環境構築・基盤（完了）
- [x] Pythonプロジェクト初期化（requirements.txt）
- [x] Cowork内蔵AI（claude CLI）動作確認・encoding対応

### Phase 2: コア機能実装（完了）
- [x] STEP1〜5 コアロジック実装
- [x] utils/sheets_client.py: Google Sheets読み書き・article_cache管理
- [x] utils/ai_client.py: claude CLIラッパー（encoding="utf-8"）
- [x] main.py: Spreadsheet URL引数対応・キャッシュ優先フロー

### Phase 3: プロンプト精緻化（完了）
- [x] サイト実データ分析（analyze_links.py）
- [x] 上位→下位リンクパターン特化プロンプトに変更
- [x] 水平方向リンクの低スコア化

### Phase 4: テスト・引き渡し（未着手）
- [ ] Googleサービスアカウント設定・Sheets API有効化
- [ ] 実Spreadsheetでのエンドツーエンドテスト
- [ ] 実データでの精度確認・閾値調整
- [ ] クライアントへの説明・デモ・本番運用開始

---

## 11. セキュリティ考慮事項

- サービスアカウントJSONはプロジェクトフォルダ外の専用フォルダに保管する
- プロジェクトフォルダ内（`claude code/`以下）に認証情報を一切残さない
- 環境変数 `GOOGLE_SERVICE_ACCOUNT` でJSONパスを参照する（コードにパスをハードコードしない）
- `.gitignore` で `.env`・`venv/`・`__pycache__/` をGit管理対象外に設定
- 外部サイトへのHTTPリクエストはGET取得のみ（書き込み・POST等は行わない）
- Google Sheets APIのスコープは `spreadsheets`（読み書き）のみに限定する

---

## 12. 将来拡張（本フェーズ対象外）

| 機能 | 概要 | 追加コスト（目安） |
|---|---|---|
| WordPressへの自動反映 | 承認済みの内部リンクをWordPress REST APIで自動設置 | ＋5〜8万円 |
| アンカーテキスト自動生成 | AIがリンクアンカーテキストまで自動提案する | ＋1万円 |
| Embedding活用 | テキストEmbeddingによる高精度な意味的関連性判定 | ＋3〜5万円 |
| Search Console連携 | GSCデータと連携してリンク効果を計測・フィードバック | ＋3〜5万円 |
