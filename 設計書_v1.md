# NYSEO内部リンクAIエージェント 設計書 v1

> 作成日: 2026-05-16 / バージョン: v1（CSV方式・ローカル実行）
> v2との違い: データ基盤がCSV、streamlit UI、単一ユーザー向けローカル実行

---

## 1. 技術スタック

| カテゴリ | 技術 | バージョン | 選定理由 |
|---|---|---|---|
| エージェント実行環境 | Cowork | 最新 | クライアント要件・ノーインフラ運用 |
| AI / LLM | Cowork内蔵AI（Claude搭載） | — | 追加API費用なし・Cowork Proプランに含まれる |
| スクリプト言語 | Python | 3.10以上 | BeautifulSoup・requests・csv全対応 |
| HTML解析 | BeautifulSoup4 + lxml | 4.12.x | 軽量・安定した記事解析・日本語対応 |
| HTTPクライアント | requests | 2.31.x | シンプルな外部URL取得・セッション管理 |
| データ基盤 | CSV（NYSEO出力ファイル） | — | 余分な認証不要・入出力がシンプル |

---

## 2. システム構成

```
[クライアント]
    ↓ NYSEOデータ（CSV）をCowork画面上で読み込む
[Cowork + Pythonスクリプト]
    ├─ STEP1: 対象記事抽出
    ├─ STEP2: 関連記事候補探索 ──→ [Cowork内蔵AI]（KW拡張）
    ├─ STEP3: 記事HTML取得・解析（requests + BeautifulSoup）
    ├─ STEP4: 関連性判定 ──→ [Cowork内蔵AI]（スコアリング）
    └─ STEP5: 結果出力
         ↓ H列以降を追記した出力CSVを生成
[出力CSV]
    ↓ クライアントが確認・最終判断
[担当者がWordPressに手動で内部リンクを設置]
```

---

## 3. モジュール構成・ディレクトリ設計

```
nyseo-internal-link-agent/
├── main.py                    # エントリーポイント・全STEPを順次実行
├── config.py                  # 設定値（閾値・列番号定義）
├── requirements.txt           # 依存パッケージ一覧
├── .gitignore
│
├── steps/
│   ├── __init__.py
│   ├── step1_extract.py       # STEP1: 対象記事抽出
│   ├── step2_search.py        # STEP2: 関連記事候補探索
│   ├── step3_fetch.py         # STEP3: 記事HTML取得・解析
│   ├── step4_judge.py         # STEP4: AI関連性判定
│   └── step5_output.py        # STEP5: CSV出力
│
├── utils/
│   ├── __init__.py
│   ├── csv_client.py          # CSV読み書きラッパー
│   └── logger.py              # ログ出力ユーティリティ
│
├── prompts/
│   ├── kw_expansion.txt       # STEP2: KW拡張プロンプトテンプレート
│   └── relevance_judge.txt    # STEP4: 関連性判定プロンプトテンプレート
│
└── docs/
    └── index.html             # GitHub Pages（クライアント向け仕様書）
```

---

## 4. データフロー

### 入力データ（CSV）

| 列 | 内容 | 型 | 役割 | 操作 |
|---|---|---|---|---|
| B列 | 記事URL | string（URL） | クロール対象URL・対象記事の識別子 | 読み取りのみ |
| C列 | メインKW（GSCクリック最多） | string | STEP2の起点キーワード | 読み取りのみ |
| E列 | 被リンク数 | integer | 対象記事の抽出条件（≤ 1） | 読み取りのみ |
| H列 | 候補記事URL① | string（URL） | システム出力：対象記事へリンクを設置する候補記事（1件目） | 書き込み |
| I列 | リンク設置箇所見出し① | string | システム出力：候補記事①内でリンクを設置する見出し箇所 | 書き込み |
| J列 | 候補記事URL② | string（URL） | システム出力：候補記事（2件目） | 書き込み |
| K列 | リンク設置箇所見出し② | string | システム出力：候補記事②内の見出し箇所 | 書き込み |
| L列 | 候補記事URL③（任意） | string（URL） | システム出力：候補記事（3件目） | 書き込み |
| M列 | リンク設置箇所見出し③（任意） | string | システム出力：候補記事③内の見出し箇所 | 書き込み |

### 中間データ（メモリ内・一時処理）

```python
# 対象記事リスト（STEP1出力）
target_articles = [
    {
        "row": 3,               # CSV行番号
        "kw": "SEO 内部リンク", # C列メインKW
        "url": "https://example.com/seo-internal-link/"
    },
    ...
]

# 候補記事解析結果（STEP3出力）
parsed_article = {
    "url": "https://example.com/article/",
    "title": "記事タイトル",
    "h1": "見出しH1",
    "h2_list": ["見出しA", "見出しB", "見出しC"],
    "h3_list": ["見出しD", "見出しE"],
    "body_text": "本文テキスト（先頭800文字程度）"
}

# AI判定結果（STEP4出力）
judgment_result = {
    "url": "https://example.com/article/",
    "score": 85,
    "reason": "検索意図が一致し、トピックの親和性が高い。",
    "recommended_heading": "内部リンクの重要性について"
}
```

---

## 5. 外部連携

| 外部サービス | 用途 | 認証 | 注意事項 |
|---|---|---|---|
| Cowork内蔵AI | STEP2 KW拡張・STEP4 関連性判定 | 不要（Cowork Proプランに含まれる） | 呼び出し間に0.5秒sleep・レート制限対応 |
| 対象サイト（HTTP） | STEP3 記事HTML取得 | なし（公開ページのみ） | タイムアウト10秒・リトライ2回・UA設定必須 |

---

## 6. エラー処理

| エラー種別 | 対応 | 処理継続 |
|---|---|---|
| HTTP取得失敗（404・タイムアウト等） | 該当記事をスキップ・警告ログ出力 | ✅ 継続 |
| Cowork内蔵AI エラー | 最大2回リトライ後、スキップ・エラーログ | ✅ 継続 |
| CSV書き込みエラー | エラーログ出力・実行停止・手動確認を求める | ❌ 停止 |
| 候補記事が2件未満 | 閾値を60→40に下げて再判定。不足なら空欄出力 | ✅ 継続 |
| B列（記事URL）が空白 | 該当行をスキップ・警告ログ | ✅ 継続 |
| H列にデータあり（処理済み） | 処理済みとして完全スキップ | ✅ 継続 |

---

## 7. AI判定ロジック設計

### 7.1 関連性スコアリング設計

| 判定観点 | 重み | 配点 | 判定内容 |
|---|---|---|---|
| 検索意図一致度 | 30% | 0〜30点 | 対象記事を読むユーザーの検索意図と候補記事が一致するか |
| トピック一致度 | 25% | 0〜25点 | 扱っているトピック・テーマが同一または密接に関連しているか |
| 文脈自然性 | 20% | 0〜20点 | 対象記事の文中で候補記事へのリンクが自然に機能するか |
| SEO妥当性 | 15% | 0〜15点 | SEO観点でリンク元→リンク先のテーマ関連性が適切か |
| 内部リンク自然性 | 10% | 0〜10点 | 具体的なアンカーテキストとしてリンクが機能するか |
| **合計** | **100%** | **0〜100点** | |

### 7.2 判定基準・閾値設定

| スコア | 評価 | 採用判定 |
|---|---|---|
| 80〜100点 | 強く関連あり | ✅ 優先採用（最上位で出力） |
| 60〜79点 | 関連あり | ✅ 通常採用 |
| 40〜59点 | やや関連あり | ⚠️ 候補が2件未満の場合のみ採用（閾値下げ時） |
| 0〜39点 | 関連なし | ❌ 除外 |

### 7.3 除外条件

- 対象記事自身のURLと同一（自己リンク防止）
- スコア < 40点（最低閾値）
- HTTP取得に失敗した記事（404・タイムアウト等）
- カテゴリページ・タグページ・アーカイブページ（URLパターン: `/category/`・`/tag/`・`/page/`）
- すでにH列以降で提案済みのURL（同一対象記事内での重複防止）

---

## 8. 処理フロー設計

### 8.1 クライアント運用フロー

| 手順 | 担当 | 操作内容 |
|---|---|---|
| ① | クライアント | Cowork画面上でNYSEOデータ（CSV）を読み込む |
| ② | クライアント | Cowork上でエージェントを起動する |
| ③ | システム（自動） | STEP1〜5を自動実行し、出力CSV（H列以降にURL・見出しを追記）を生成する |
| ④ | クライアント | 出力CSVのH列以降を確認し、提案内容をレビューする |
| ⑤ | クライアント | レビューOKの内容をもとに、H列の候補記事（WordPressの当該記事）を開き、I列の見出し箇所に対象記事へのリンクを手動で設置する |
| ⑥ | クライアント | 再実行が必要な場合、出力CSVのH列以降のデータを削除した上で再取り込みし、エージェントを再起動する |

---

## 9. 開発フェーズ

### Phase 1: 環境構築・基盤（完了）
- [x] Pythonプロジェクト初期化（requirements.txt）
- [x] CSV読み取り・書き込み・文字コード動作確認
- [x] Cowork内蔵AI動作確認

### Phase 2: コア機能実装（完了）
- [x] STEP1: 対象記事抽出ロジック実装
- [x] STEP2: KW探索・Cowork内蔵AIによるKW拡張実装
- [x] STEP3: 記事HTML取得・BeautifulSoupパース実装
- [x] STEP4: Cowork内蔵AIによる関連性スコアリング実装
- [x] STEP5: CSVへの書き込み実装

### Phase 3: エラー処理・安定化（完了）
- [x] HTTPタイムアウト・エラー処理実装
- [x] AIリトライ・レート制限対応
- [x] H列チェックによる再処理スキップ実装
- [x] ログ出力整備

---

## 10. セキュリティ考慮事項

- APIキー・認証情報は一切不要（Cowork内蔵AIを使用するため）
- `.gitignore` で機密ファイル（`.env`等）をGit管理対象外に設定
- CSVファイルはローカル環境またはCowork内のみで取り扱い、外部共有しない
- 外部サイトへのHTTPリクエストはGET取得のみ（書き込み・POST等は行わない）
- User-Agentに識別可能な文字列を設定（`NYSEOInternalLinkAgent/1.0`）

---

## 11. 将来拡張（本フェーズ対象外）

| 機能 | 概要 | 追加コスト（目安） |
|---|---|---|
| WordPressへの自動反映 | 承認済みの内部リンクをWordPress REST APIで自動設置 | ＋5〜8万円 |
| アンカーテキスト自動生成 | AIがリンクアンカーテキストまで自動提案する | ＋1万円 |
| Embedding活用 | テキストEmbeddingによる高精度な意味的関連性判定 | ＋3〜5万円 |
| Search Console連携 | GSCデータと連携してリンク効果を計測・フィードバック | ＋3〜5万円 |
| 複数ユーザー対応・共有 | Google Spreadsheetsを使った複数人での結果共有（→ v2） | 別途見積もり |
