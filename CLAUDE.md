# NYSEO 内部リンク構築AIエージェント — Cowork起動指示書

## このエージェントの目的
NYSEOから出力したCSVを読み込み、被リンク不足記事（E列≤1）に対して
関連性の高い他記事から内部リンクを設置する候補を自動提案する。

## 実行手順

1. `input/` フォルダにNYSEOから出力したCSVが配置されていることを確認する
2. 以下のコマンドで依存パッケージをインストールする（初回のみ）:
   ```
   pip install -r requirements.txt
   ```
3. メインスクリプトを実行する:
   ```
   python main.py
   ```
4. 処理完了後、`output/` フォルダに出力CSVが生成される

## 入力CSV形式（NYSEO出力）
- A列: NO（連番）
- B列: 記事URL
- C列: メインKW（GSCクリック最多）
- D列: 発リンク数
- E列: 被リンク数（≤1が処理対象）
- F列: クリック数
- G列: 表示回数

## 出力CSV形式（H列以降を追記）
- H列: 候補記事URL①（この記事から対象記事へ内部リンクを設置する）
- I列: リンク設置箇所見出し①
- J列: 候補記事URL②
- K列: リンク設置箇所見出し②
- L列: 候補記事URL③（任意）
- M列: リンク設置箇所見出し③（任意）

## 再実行する場合
出力CSVのH〜M列のデータを削除してから `input/` に配置し直して再実行する。

## ルール
- APIキー・認証情報は不要（Cowork内蔵AIを使用）
- 外部サイトへのHTTPリクエストはGET取得のみ
- エラーが発生した記事はスキップして処理を継続する
