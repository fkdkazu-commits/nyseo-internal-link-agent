# NYSEO 内部リンク構築エージェント — Cowork指示書

## このプロジェクトについて

Google SpreadsheetのSEOデータを読み込み、被リンク不足記事に内部リンク候補を自動提案し、WordPress記事への自動挿入まで行うツール群です。

---

## 実行できるコマンド

### 1. 内部リンクエージェント（メイン処理）
```
py main.py <SpreadsheetURL> [オプション]
```
SpreadsheetのH〜M列に内部リンク候補（候補記事URL・見出し）を書き込みます。

オプション:
- `--limit N`       : N件のみ処理（テスト用）
- `--row N`         : 指定行のみ処理
- `--rows N,M,...`  : 複数行を強制再処理
- `--reprocess-all` : 全行を強制再処理
- `--api`           : APIモード（高速・並列処理）
- `--mid`           : 中精度モード（--api と併用）

### 2. WordPress内部リンク挿入ツール（v1.3〜）
```
py main_wp.py <SpreadsheetURL> [--link blogcard|url|atag] [--row N] [--limit N]
```
SpreadsheetのH〜M列（承認済みの内部リンク候補）を読み込み、WordPress記事に自動挿入します。

オプション:
- `--link blogcard|url|atag`  : リンク形式（デフォルト: url）
- `--row N`          : 指定行のみ処理
- `--limit N`        : No N までのみ処理（テスト用）
- `--editor auto|classic|gutenberg` : エディタ形式（デフォルト: auto＝記事ごとに自動判定）

---

## ユーザーからの指示パターンと対応

### パターン1：SpreadsheetのURLが渡された

**直前の会話の状態・文脈に関わらず、SpreadsheetのURLが届いたら必ず新規実行として以下のフローを実行する。statusの確認・ポーリングは行わない。**

1. ローカルランナー経由で内部リンクエージェントを起動する（H列に値がある行は自動スキップされる）
2. 起動後、以下を伝える：
   > 「内部リンクエージェントを起動しました。処理が完了したらSpreadsheetのH〜M列に候補が書き込まれます。ターミナルで完了メッセージが出たら、ここに「完了しました」と送ってください。」
3. **ユーザーから「完了しました」が送られてきたら、必ず以下を確認する：**
   > 「WordPressへの自動挿入も続けますか？
   > ・**はい** → スプレッドシートを確認せずにそのまま挿入します
   > ・**いいえ** → スプレッドシートでH〜M列を確認・承認してから、後でWordPress挿入を実行してください」

4. **「はい」が選択されたら、パターン2の確認①②③と同じ手順でWP挿入を起動する（サイト確認→リンク形式確認→実行範囲確認→run-wp呼び出し）**

### パターン2：「WordPressに挿入してほしい」「WP挿入を実行して」

Spreadsheetのレビュー・承認が完了している前提で実行する。以下を確認してから起動する：

以下の確認①②③を**必ずこの順番で**すべて行ってから run-wp を呼ぶ。途中でユーザーの返答が来ても、残りの確認が終わるまで起動しない。

**確認①：対象サイト（複数サイト登録時のみ）**

まず `http://127.0.0.1:8765/sites` を開いてサイト一覧を取得する。
- サイトが1件のみ → 確認不要。そのまま確認②へ。
- サイトが2件以上 → 以下を確認する：
> 「どのWordPressサイトに挿入しますか？
> ・site-a.com
> ・site-b.com」

**確認②：リンク形式**
> 「挿入するリンクの形式を教えてください：
> ・URL挿入（Gutenberg・クラシック自動判定。URLを挿入しWordPressのoEmbedでブログカード表示を試みる。表示結果はテーマに依存します）
> ・aタグ（`<a href="URL">記事タイトル</a>` の形式。Gutenberg・クラシック両対応。確実にテキストリンク）
> ・ショートコード（`[related id={id} label=xxx]` のような独自形式。`{id}` にスプレッドシートA列の投稿IDが自動で入る。テンプレートはターミナルで入力）」

※エディタ形式（クラシック / Gutenberg）は記事ごとに自動判定されるため確認不要。
※ショートコードを選択した場合、ターミナルでテンプレート文字列の入力を求められる。

**確認③：実行範囲 ← 必ず確認①②の後に聞く。スキップ禁止。**
> 「まず1記事のみテスト実行しますか？それとも一括実行しますか？
> ・1記事のみ → 先頭の未処理行を1件だけ処理します。表示を確認してから一括実行してください。
> ・一括実行 → すべての未処理行を処理します。」

確認①②③がすべて揃ったら、ローカルランナー経由でWP挿入ツールを起動する。

### パターン3：「内部リンクエージェントを実行して、そのままWP挿入もして」

1. 内部リンクエージェントを起動する
2. 起動後、以下を伝える：
   > 「内部リンクエージェントを起動しました。ターミナルで完了メッセージが出たら、ここに「完了しました」と送ってください。」
3. ユーザーから「完了しました」が送られてきたら、以下を警告する：
   > 「スプレッドシートのH〜M列を確認せずにWordPress挿入を行います。問題ありませんか？」
4. ユーザーが了承したら、リンク形式と実行範囲を確認してWP挿入ツールを起動する

---

## ローカルランナー経由での起動方法

ツールはローカルランナーサーバー（ポート8765）経由で起動する：

```
# 内部リンクエージェント起動
GET http://127.0.0.1:8765/run?url=<SpreadsheetURL>

# WordPress挿入ツール起動（v1.3〜）
GET http://127.0.0.1:8765/run-wp?url=<SpreadsheetURL>&link=<url|atag|shortcode>&site=<domain>&count=<1|省略>
# count=1: 先頭の未処理行を1件のみ処理（テスト実行）
# count省略: 全未処理行を一括処理
# link=shortcode の場合: ターミナルでテンプレート文字列の入力を求められる（{id} にA列の投稿IDが入る）

# 登録済みWordPressサイト一覧取得
GET http://127.0.0.1:8765/sites

# 実行状況確認
GET http://127.0.0.1:8765/status

# 停止
GET http://127.0.0.1:8765/stop

# 実行中フラグの強制リセット（処理は終わっているのに running のままスタックした場合）
GET http://127.0.0.1:8765/reset
```

### status が "running" のままスタックした場合

処理完了・PowerShell終了済みなのに `/status` が `running` を返す場合は、以下でフラグをリセットしてから WP 挿入を続行する：

```
GET http://127.0.0.1:8765/reset
```

reset 後は通常通り `/run-wp` を呼ぶ。

### run-wp が `site_required` を返した場合

`{"status": "site_required", "sites": ["site-a.com", "site-b.com"]}` が返った場合は、
ユーザーにどのサイトか確認したうえで、`site` パラメータを付けて再度 run-wp を呼ぶ：

```
GET http://127.0.0.1:8765/run-wp?url=<SpreadsheetURL>&link=<url|atag|blogcard>&site=site-a.com
```

`site` パラメータを付けた場合は `site_required` は返らず、`{"status": "started"}` が返る。

---

## 開発ルール

- **本番用コードとテスト用コードは明確に分ける**
  - 本番用: `main.py` / `main_wp.py` / `steps/` / `utils/`
  - テスト用: `test_*.py` / `reset_wp_test.py` / `clean_test_articles.py` / `prepare_test_rows.py` / `test_convert_gutenberg.py`
  - テスト専用の機能・オプションを本番コードに混入させない

- **根拠が薄い対応・その場限りの判断は入れない。必ずユーザーに確認してから実装する**
  - 「〜と思われる」「〜のはず」という推測だけで実装しない
  - 元データの内容・仕様を決めつけない（例：「CTA」「不要なテキスト」などの判断）
  - 変更の影響範囲が不明な場合は実装前に確認を取る

---

## 注意事項

- **レビューを省略した連続実行はリスクがある**。AIの判定結果が100%正しいとは限らないため、原則としてSpreadsheetのH〜M列を確認してからWP挿入を実行することを推奨する
- WordPressへの挿入は取り消しが手間のため、不明な点があれば実行前にユーザーに確認する
- N列に「済み」が記録されている行は重複挿入されない（冪等性あり）
