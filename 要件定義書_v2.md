# NYSEO内部リンクAIエージェント 要件定義書 v2

> 作成日: 2026-05-17 / バージョン: v2（Google Sheets方式・複数ユーザー共有）
> v1との違い: データ基盤がGoogle Spreadsheet、複数ユーザーでSpreadsheetを共有、article_cacheタブによるHTMLキャッシュ

---

## 1. プロジェクト概要

### 目的
NYSEOデータを格納したGoogle Spreadsheetを読み込み、被リンク不足記事（被リンク数E列≤1）に対して、関連性の高い他記事から内部リンクを設置する候補を自動提案し、SEO内部リンク構築業務を半自動化する。

### 主要機能
- 被リンク不足記事の自動抽出（E列 ≤ 1）
- メインKW（C列）を起点とした関連記事探索（KW一致・部分一致・関連語・上位概念）
- C列空欄時はSTEP3取得済みのh2/h3見出しをKWとして自動使用
- 候補記事のHTML解析（title / h1 / h2 / h3 / 本文）
- article_cacheタブによるHTML解析結果のセッション間共有・再利用
- Cowork内蔵AIによる「上位概念→下位詳細」パターン特化スコアリング（100点満点）
- 内部リンク設置候補をSpreadsheetのH列以降に書き込み

> **本システムは「提案型」MVPです。最終的な内部リンク設置はクライアント担当者様が確認・判断することを想定しています。WordPressへの自動反映は本フェーズでは対象外となります。**

---

## 2. ユーザー・権限定義

| ロール | 説明 | できること | できないこと |
|---|---|---|---|
| システム運用者（エージェント実行者） | Cowork上でエージェントを起動する | エージェント実行・再実行・ログ確認 | WordPressへの直接投稿（本フェーズ対象外） |
| クライアント担当者（レビュアー） | 提案結果を確認・レビューする | SpreadsheetのH列以降確認・内部リンク実施の最終判断 | エージェントの直接操作 |

> 複数のユーザーが同一Spreadsheetを共有することで、結果を全員で参照できる。

---

## 3. 機能要件

### 3.1 STEP1 — 対象記事抽出
- SpreadsheetのE列（被リンク数）を全行読み取る
- E列 ≤ 1 かつ H列が空白の行を対象に選定する
- 対象行のB列（記事URL）・C列（メインKW）を取得する
- C列が空白の場合は「auto_detect」フラグを立て、STEP3取得後にh2/h3見出しをKWとして使用する
- B列が空の場合のみその行をスキップしてログに記録する

### 3.2 STEP2 — 関連記事候補探索
- メインKW（C列またはh2/h3から自動検出）とSpreadsheet内の全記事KWをKW完全一致・部分一致で比較する
- Cowork内蔵AIを用いてKWの上位概念・関連語を生成し探索範囲を拡張する
- 対象記事自身のURL・すでに採用済みのURLは除外する
- スコア順に最大20件の候補リストを生成してSTEP3へ渡す

### 3.3 STEP3 — 候補記事取得・解析（article_cacheタブ優先）
- **まずarticle_cacheタブ（Spreadsheet2枚目）を確認し、キャッシュ済みURLはHTML取得をスキップする**
- キャッシュがない場合のみHTTPSリクエストを送信する（タイムアウト10秒）
- BeautifulSoupで以下を抽出する
  - `<title>` タグ
  - `<h1>` タグ
  - `<h2>` / `<h3>` タグ一覧
  - 本文テキスト（先頭1500文字程度）
- 取得成功時はarticle_cacheタブに保存する（次回・他ユーザーの実行で再利用）
- HTTP失敗（404・タイムアウト等）はスキップ・警告ログ出力・処理継続する
- 最大2回リトライ後に失敗した場合は候補から除外する

### 3.4 STEP4 — AI関連性判定（上位概念→下位詳細パターン特化）
- Cowork内蔵AIを使用し、「上位概念から下位詳細への内部リンク」パターンに特化して100点満点でスコアリングする
- 評価軸:
  - 候補記事が対象記事の上位概念であること（40点）
  - 対象記事の内容についてさらに詳しく知りたい読者を自然に誘導できること（30点）
  - 候補記事の文脈内に対象記事のトピックが自然に含まれること（30点）
- スコア ≥ 60点の記事を採用候補とする
- 候補が2件未満の場合は閾値を40点に下げて再判定する
- 採用候補から「貼る箇所の見出し」もAIが合わせて提案する

### 3.5 STEP5 — 内部リンク設置候補生成・Spreadsheet書き込み
- 採用候補をスコア降順で並び替え、上位2〜3件をSpreadsheetに書き込む
- 出力列:
  - H列: 候補記事URL①
  - I列: リンク設置箇所見出し①
  - J列: 候補記事URL②
  - K列: リンク設置箇所見出し②
  - L列: 候補記事URL③（任意）
  - M列: リンク設置箇所見出し③（任意）

---

## 4. ビジネスロジック・業務ルール

### 抽出ルール
- E列 ≤ 1（E列が空白の場合も対象に含める）
- 同一URLの重複処理は行わない

### 候補生成ルール
- 同一URLへの重複提案は除外する
- 対象記事自身のURLは除外する
- 提案は最低2件・最大3件（MVPフェーズ）
- 水平方向の同テーマ記事（例: 品川の風俗街↔鶯谷の風俗街）はスコアを低くする

### キャッシュルール
- article_cacheタブに記録されているURLはHTTPリクエストをスキップする
- 全記事のHTML取得は初回のみ（以降は全ユーザー共通でキャッシュを再利用）
- キャッシュは手動削除以外でリセットされない（データが古くなる場合は手動で行を削除）

### 出力保護ルール
- H列にデータがある行はスキップする（再取り込み時も同様）

---

## 5. 非機能要件

| 項目 | 内容 |
|---|---|
| 対象デバイス | PC（Cowork実行環境） |
| デザイン優先度 | 低（出力はSpreadsheet） |
| 同時利用人数 | 複数名（Spreadsheet共有による結果の共有） |
| 処理速度 | 1記事あたり最大30秒以内（AI・HTTP取得込み）/ キャッシュ命中時は大幅短縮 |
| 認証方式 | Googleサービスアカウント（JSONファイル・プロジェクトフォルダ外に保管） |
| 通信 | HTTPS（Google Sheets API・外部URL取得） |
| エラー耐性 | 個別記事のHTTP失敗は警告ログを出してスキップ。全体処理は継続する |
| デプロイ先 | Cowork（クラウド実行環境） |

---

## 6. 制約条件

| 項目 | 内容 |
|---|---|
| インフラ | AWSなどの大規模インフラは使用しない |
| 実行環境 | Cowork上で完結させる |
| 言語 | Python 3.10以上（BeautifulSoup / requests / gspread / google-auth） |
| AI | Cowork内蔵AI（Claude搭載） |
| データ基盤 | Google Spreadsheet（NYSEO出力データ＋article_cacheタブ） |
| 認証情報管理 | サービスアカウントJSONはプロジェクトフォルダ外の専用フォルダに保管。環境変数 `GOOGLE_SERVICE_ACCOUNT` で参照する |
| WordPress自動投稿 | 本フェーズ対象外 |
| アンカーテキスト自動生成 | 本フェーズ対象外 |
| Embedding活用 | 本フェーズ対象外 |

---

## 7. 実行方法

### 初回セットアップ（Cowork側）
```
# 依存パッケージのインストール（初回のみ）
pip install -r requirements.txt

# サービスアカウントJSONのパスを環境変数に設定
$env:GOOGLE_SERVICE_ACCOUNT = "C:\path\outside\project\service_account.json"
```

### 実行コマンド
```
py main.py <SpreadsheetURL>
```

例:
```
py main.py https://docs.google.com/spreadsheets/d/xxxxxxxx
```

### Spreadsheet準備事項
- 1枚目のシート（Sheet1）にNYSEOデータを配置する
- サービスアカウントのメールアドレスをSpreadsheetの「共有」に追加する（編集者権限）
- 2枚目のarticle_cacheタブはシステムが自動作成する（手動作成不要）

---

## 8. クライアント側の準備事項

| 項目 | 内容 | 確認状況 |
|---|---|---|
| Cowork環境 | Coworkアカウント（Claude Pro）の用意・エージェント実行権限の確認 | 要確認 |
| Google Spreadsheet | NYSEOから出力したデータをSpreadsheetに貼り付け済みであること | 要確認 |
| サービスアカウント | Googleサービスアカウント作成・Sheets API有効化・JSONファイル取得 | 要確認 |
| Spreadsheet共有 | サービスアカウントのメールアドレスをSpreadsheetに編集者として共有 | 要確認 |
| 記事のアクセス可否 | B列の記事URLがHTTPSで公開済みであること（非公開・Basic認証不可） | 要確認 |

---

## 9. 将来拡張（本フェーズ対象外）

| 機能 | 概要 | 追加コスト（目安） |
|---|---|---|
| WordPressへの自動反映 | 承認済みの内部リンクをWordPress REST APIで自動設置 | ＋5〜8万円 |
| アンカーテキスト自動生成 | AIがリンクアンカーテキストまで自動提案する | ＋1万円 |
| Embedding活用 | テキストEmbeddingによる高精度な意味的関連性判定 | ＋3〜5万円 |
| Search Console連携 | GSCデータと連携してリンク効果を計測・フィードバック | ＋3〜5万円 |
